# POS 录单视频录制指南

这个文件用于指导在 Windows 电脑上录制伯俊 POS 手动录单全过程。录制视频会帮助后续实现自动化脚本时判断界面流程、控件位置、弹窗行为，以及哪些步骤需要使用坐标或图片识别。

## 录制前准备

录制前请尽量固定环境：

- POS 窗口最大化
- 记录 Windows 屏幕分辨率
- 记录 Windows 缩放比例，例如 `100%` 或 `125%`
- 记录 POS 版本号，如果界面中能看到
- 使用测试订单，尽量避免录到真实会员、手机号、支付账号等敏感信息
- 如果必须出现敏感信息，后续分享视频前请打码

建议同时记录：

```text
Windows 版本：
屏幕分辨率：
Windows 缩放比例：
POS 是否最大化：
POS 版本号：
录制时间：
测试订单号：
```

## 必须录制的完整流程

请从桌面开始录制，完整包含以下步骤。

### 1. 启动和登录

1. 从桌面打开伯俊 POS 系统。
2. 展示登录界面。
3. 如果用户名和密码已保存或已填写，直接点击登录。
4. 登录后展示 POS 主界面。

### 2. 进入收银台

1. 在主界面点击 `收银台`。
2. 展示进入收银台后的首页。

### 3. 选择订单日期

1. 展示收银台界面的日期控件位置。
2. 打开日期下拉选择框。
3. 选择和订单号前 8 位相同的日期。
4. 展示日期选择完成后的界面状态。

### 4. 输入数量为 1 的商品

1. 复制或输入一个商品条码到条码输入区域。
2. 按回车。
3. 展示商品进入商品列表后的状态。
4. 该商品数量为 `1`，不需要修改数量，请保持默认数量。

### 5. 输入数量大于 1 的商品

1. 复制或输入另一个商品条码到条码输入区域。
2. 按回车。
3. 展示商品进入商品列表后的状态。
4. 找到该商品条码所在记录中的数量输入框。
5. 将数量修改为 Excel 中对应的数量。
6. 按回车确认。
7. 展示数量修改成功后的状态。

### 6. 全选商品

1. 展示商品列表头部的 `全选` radio。
2. 点击 `全选` radio。
3. 展示所有商品被选中后的状态。

这个步骤很重要，因为该 radio 目前确认无法通过快捷键或控件定位触发，后续很可能需要坐标点击或图片识别。

### 7. 修改订单总价

1. 按 `F11`。
2. 展示弹出的修改订单总价窗口。
3. 展示窗口内三个选项。
4. 点选 `总额改价`。
5. 输入 Excel 中的 `订单实收` 作为订单总金额。
6. 按回车关闭改价框。
7. 展示改价完成后的收银台界面。

### 8. 去收银和取消会员输入

1. 点击 `去收银`。
2. 展示弹出的会员输入框。
3. 点击 `取消`。
4. 展示取消后的界面状态。

### 9. 生成订单和调取小票打印

1. 按第一次回车，生成订单。
2. 等待大约 `0.5s`。
3. 按第二次回车，调取小票打印。
4. 展示弹出的 Windows 系统打印框。
5. 手动关闭打印框。
6. 展示关闭打印框后 POS 回到收银台首页的状态。

### 10. 浏览器结果校验

如果方便，请继续录制浏览器查询结果的流程：

1. 打开浏览器。
2. 访问用于查询录单结果的地址。
3. 如果需要登录，展示登录流程。分享视频前注意隐藏敏感信息。
4. 输入查询条件，例如订单号、日期、金额等。
5. 展示查询结果页面。
6. 说明如何判断该订单录入成功。

## 录制建议

- 尽量录制完整屏幕，不要只录 POS 窗口。
- 鼠标移动速度稍微慢一点，方便看清点击位置。
- 点击关键控件前可以停顿半秒，例如日期、全选 radio、总额改价、去收银、取消按钮。
- 修改数量和修改订单总金额时，尽量让输入前后的值都清楚可见。
- 如果某一步出现弹窗、加载、卡顿或错误，也请保留在视频中。
- 如果一单流程太长，可以先录一个完整订单，再单独补录浏览器校验流程。

## 分享给开发时一并提供的信息

视频之外，请同时提供：

```text
Windows 版本：
屏幕分辨率：
Windows 缩放比例：
POS 是否最大化：
POS 版本号：
测试订单号：
测试订单日期：
是否包含数量大于 1 的商品：
浏览器校验地址：
浏览器校验是否需要登录：
录制过程中遇到的异常：
```
